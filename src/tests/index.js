// import React from 'react';
// import { shallow, mount } from 'enzyme';
// import BalanceText from '../index';
// import { expect } from 'chai';
// import sinon from 'sinon';
const {describe} = global;

describe('BalanceText', () => {
    // There are no unit tests right now.  This is a very hard
    // component to unit test since it relies so muc on DOM manipulation.
    // But this his here to keep the testing framework happy, for now.
});
